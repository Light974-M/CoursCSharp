using System;

namespace TableauExosSuite
{
    public class ExosCoursSuite
    {
        static int[] AddValues(int[] tabValues, int val, int indice)
        {
            int[] tabAdd;
            tabAdd = new int [tabValues.Length+1];

            for(int i = 0; i < tabValues[i], )
            {
                
            }
        }
    }
}